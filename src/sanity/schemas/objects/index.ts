import contentBlockItem from './contentBlockItem';
import linkList from './linkList';
import productGridItem from './productGridItem';

export const objectTypes = [contentBlockItem, productGridItem, linkList];

// Optional: export individual types if needed elsewhere, though usually importing from array is enough for schema definition
// export { contentBlockItem };
